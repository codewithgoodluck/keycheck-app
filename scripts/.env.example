# Same service account key used by the WhatsApp bot — reuse the same file.
FIREBASE_SERVICE_ACCOUNT_PATH=../whatsapp-bot/serviceAccountKey.json

# Optional: post a Slack message when efcc-monitor.js finds new candidates.
# Leave blank to skip — results still get written to pending-review.json either way.
SLACK_WEBHOOK_URL=
